{
  "name": "partyspark",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot --inspect index.ts",
    "build": "bun build index.ts --outdir dist",
    "preview": "bun run dist/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}